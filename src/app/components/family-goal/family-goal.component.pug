section.col-xs-2.no-padding
  app-desktop-bar([adminId]="uid")
section.col-xs-10
  app-back
  section.row 
    svg.familyGoal-header(width='100vw' viewBox='0 0 1085 296')
      path.familyGoal-path(d="M.2.4V242.1S514,358,1085,242.1V0Z")
    h1.familyGoal-title Premio familiar
  section.row.col-xs-0.col-md-0.col-lg-3
  section.row
    section.col-xs-12.col-md-6.col-lg-6.form-container
      form([formGroup]="goalForm")
        h4.titles.main Que deseas dar de premio familiar?
          .form-group([ngClass]="{'has-danger': goalForm.controls.titleGoal.invalid && goalForm.controls.titleGoal.dirty, 'has-success': goalForm.controls.titleGoal.valid && goalForm.controls.titleGoal.dirty}")
            input.form-control.newGoalInput(type='text', placeholder="Salida al cine", [(ngModel)]="titleGoal", formControlName="titleGoal", required)
        h4.titles Describe el premio
          section.col-xs-12
            textarea(placeholder="El sabado todos vamos ir al cine porque completamos todas las tareas de la semana!",[(ngModel)]="descriptionGoal")
      .btn-add
        .eff-4
        button.btnGoal((click)="sendFamGoal(titleGoal,descriptionGoal)", [disabled]="goalForm.invalid")  Agregar 
  section.row.col-xs-0.col-md-0.col-lg-3
